name: Deploy standalone page to GitHub Pages

on:
  push:
    branches: [gh-pages]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stage site (both planners + landing page)
        run: |
          mkdir -p _site
          cp standalone-pages/index.html \
             standalone-pages/compatibility.html standalone-pages/app.js \
             standalone-pages/farm-planner.html standalone-pages/farm-app.js \
             standalone-pages/farm-styles.css \
             standalone-pages/styles.css standalone-pages/data.js _site/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
